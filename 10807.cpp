/*#include <iostream>
#include <vector>

using namespace std;

int main(void)
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n;
    cin >> n;
    vector<int> v(n);
    int x;
    cin >> x;
    int count = 0;
    for(int i = 0;i<v.size();i++)
        if(v[i] == x)
            count++;
    cout << count;
    return 0;
}*/
